<% if trade_card_offer.persisted? %>
  <% form_url = trade_card_offer_path(@trade.event, trade_card_offer) %>
  <% form_method = :patch %>
<% else %>
  <% form_url = trade_card_offers_path(@trade.event) %>
  <% form_method = :post %>
<% end %>

<% frame_id ||= "new_trade_card_offer" %>

<%= form_with url: form_url, method: form_method, local: true, data: { turbo: true } do |f| %>
  <!-- Hidden trade_id for admin operations only -->
  <% if current_user.role_admin? %>
    <%= hidden_field_tag :trade_id, @trade.id %>
  <% end %>

  <% if trade_card_offer.errors.any? %>
    <div id="error_explanation" class="bg-red-50 border border-red-200 rounded-lg p-4 mb-4">
      <h2 class="text-red-800 font-semibold mb-2">
        <%= pluralize(trade_card_offer.errors.count, "error") %> prohibited this record from being saved:
      </h2>
      <ul class="list-disc list-inside space-y-1 text-red-700">
        <% trade_card_offer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="space-y-4">
    <!-- Card Name -->
    <div>
      <%= f.label :card_name, "カード名", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.text_field :card_name, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- Quantity -->
    <div>
      <%= f.label :quantity, "数量", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.number_field :quantity, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- Language -->
    <div>
      <%= f.label :language, "言語", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.select :language, TradeCardOffer.languages.keys.map { |k| [I18n.t("activerecord.enums.trade_card_offer.language.#{k}"), k] }, { prompt: "選択してください" }, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- Condition -->
    <div>
      <%= f.label :condition, "状態", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.select :condition, TradeCardOffer.conditions.keys.map { |k| [I18n.t("activerecord.enums.trade_card_offer.condition.#{k}"), k] }, { prompt: "選択してください" }, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- Foil -->
    <div>
      <%= f.label :foil, "フォイル", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.select :foil, TradeCardOffer.foils.keys.map { |k| [I18n.t("activerecord.enums.trade_card_offer.foil.#{k}"), k] }, { prompt: "選択してください" }, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- Frame -->
    <div>
      <%= f.label :frame, "フレーム", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.select :frame, TradeCardOffer.frames.keys.map { |k| [I18n.t("activerecord.enums.trade_card_offer.frame.#{k}"), k] }, { prompt: "選択してください" }, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- PW Mark -->
    <div>
      <%= f.label :pw_mark, "PWマーク", class: "block text-xs font-bold text-stone-500 mb-2" %>
      <div class="flex items-center gap-4">
        <%= f.radio_button :pw_mark, true %>
        <%= f.label :pw_mark_true, "あり", for: "trade_card_offer_pw_mark_true", class: "text-xs font-bold text-stone-700" %>
        <%= f.radio_button :pw_mark, false %>
        <%= f.label :pw_mark_false, "なし", for: "trade_card_offer_pw_mark_false", class: "text-xs font-bold text-stone-700" %>
      </div>
    </div>

    <!-- Expansion -->
    <div>
      <%= f.label :expansion_id, "セット", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.collection_select :expansion_id, Expansion.all, :id, :name, { prompt: "選択してください" }, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- Note -->
    <div>
      <%= f.label :note, "備考", class: "block text-xs font-bold text-stone-500 mb-1" %>
      <%= f.text_area :note, rows: 3, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
    </div>

    <!-- Amount (admin only) -->
    <% if current_user.role_admin? %>
      <div>
        <%= f.label :amount, "金額 (管理者のみ)", class: "block text-xs font-bold text-stone-500 mb-1" %>
        <%= f.number_field :amount, class: "w-full px-3 py-2 border border-stone-300 rounded-md text-xs focus:outline-none focus:ring-2 focus:ring-accent" %>
      </div>
    <% end %>

    <!-- Submit buttons -->
    <div class="flex gap-2 pt-4">
      <%= f.submit trade_card_offer.persisted? ? "更新" : "追加", class: "px-4 py-2 bg-offer text-white rounded-md hover:opacity-90 font-bold text-xs" %>
      <button type="button" class="px-4 py-2 bg-white text-stone-800 border border-stone-300 rounded-md hover:bg-stone-50 font-bold text-xs"
              onclick="document.getElementById('<%= dom_id(trade_card_offer, :edit_form) %>').style.display = 'none'">
        キャンセル
      </button>
    </div>
  </div>
<% end %>
