<div>
  <%= card.label :name %>
  <%= card.text_field :name %>
</div>

<div>
  <%= card.label :expansion %>
  <%= card.text_field :expansion %>
</div>

<div>
  <%= card.label :foil %>
  <%= card.checkbox :foil %>
</div>

<div>
  <%= card.label :language %>
  <%= card.collection_select :language, Wishlist.languages, :last, :first %>
</div>

<div>
  <%= card.label :edition %>
  <%= card.collection_select :edition, Wishlist.editions, :last, :first %>
</div>

<div>
  <%= card.label :state %>
  <%= card.collection_select :state, Wishlist.states, :last, :first %>
</div>
